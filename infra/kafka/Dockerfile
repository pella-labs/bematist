# Bematist — Kafka broker (Bitnami) in KRaft mode for Railway dev.
# Named "redpanda" for historical reasons (the private domain is locked).
# Configured entirely via KAFKA_CFG_* env vars on the service.
FROM bitnami/kafka:3.7
