// Bematist — PgBoss worker entrypoint.
// PgBoss is for crons only (CLAUDE.md Architecture Rule #4). Real job handlers
// land in Sprint 1+ alongside the partition-drop GDPR worker (Jorge).
export const placeholder = "worker-skeleton";
